import { FullContext, RpcContext } from 'packages/sdk/src/context'
import { CreateMarketParams } from './types'

export const create = async (
  context: RpcContext | FullContext,
  params: CreateMarketParams,
) => {
  return {}
}
