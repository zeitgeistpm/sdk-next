{
  "name": "sdk-v2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test"
  },
  "packageManager": "yarn@3.2.2",
  "devDependencies": {
    "@open-web3/dev-config": "^0.2.3",
    "@open-web3/util": "^2.0.1",
    "turbo": "^1.4.3",
    "typescript": "^4.8.2"
  }
}
